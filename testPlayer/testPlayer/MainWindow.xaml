<Window x:Class="testPlayer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:testPlayer"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    
    


    <StackPanel VerticalAlignment="Center" Margin="20">
        
        
        <!-- Текст трека -->
        <wv2:WebView2 x:Name="HiddenBrowser" 
                      Width="1" 
                      Height="1" 
                      
                      HorizontalAlignment="Left" 
                      VerticalAlignment="Top" 
                      Margin="-3,0,0,0"/>

        <TextBlock x:Name="trackName" Text="penis" HorizontalAlignment="Center" Margin="0,0,0,10" FontSize="16"/>
        


        <TextBlock x:Name="TxtStatus" Text="Нажмите Play" HorizontalAlignment="Center" Margin="0,0,0,10" FontSize="16"/>

        <!-- Таймбар -->
        <TextBlock x:Name="TxtTime" Text="00:00 / 00:00" HorizontalAlignment="Right" Margin="0,0,5,0" FontSize="14" Foreground="Gray"/>
        
        <Slider x:Name="TimelineSlider" Minimum="0" Maximum="100" Margin="0,10" 
            Thumb.DragStarted="TimelineSlider_DragStarted" 
            Thumb.DragCompleted="TimelineSlider_DragCompleted"
                ValueChanged="TimelineSlider_ValueChanged"
                PreviewMouseLeftButtonDown="TimelineSlider_PreviewMouseLeftButtonDown"/>

        <!-- Кнопки -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="⏮ Prev" Width="60" Margin="5" Click="BtnPrev_Click"/>
            <Button x:Name="BtnPlay" Content="▶ Play" Width="80" Height="40" Margin="5" Click="BtnPlay_Click"/>
            <Button Content="⏭ Next" Width="60" Margin="5" Click="BtnNext_Click"/>
        </StackPanel>
    </StackPanel>
    
    
    

</Window>
