<Page x:Class="MusicAppFront.Views.Pages.InfoPlaylistPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MusicAppFront.Views.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="InfoPlaylistPage">

    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Hidden" 
                      HorizontalScrollBarVisibility="Disabled">
            <StackPanel Margin="40,20">
                <Grid Margin="0,0,0,30">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Width="150" 
                            Height="150">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="40" 
                                              Color="Black" 
                                              Opacity="0.6" 
                                              ShadowDepth="0"/>
                        </Border.Effect>
                        <Border CornerRadius="25" 
                                ClipToBounds="True"
                                Background="#1A1A1E">
                            <Image Source="{Binding ImageUrl}" 
                                   Stretch="UniformToFill"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"/>
                        </Border>
                    </Border>

                    <StackPanel Grid.Column="1" 
                                VerticalAlignment="Bottom" 
                                Margin="35,0,0,10">
                        <TextBlock Text="АЛЬБОМ" 
                                   Foreground="White" 
                                   FontSize="13" 
                                   FontWeight="Bold" 
                                   Opacity="0.5"/>
                        <TextBlock Text="{Binding Name}" 
                                   Foreground="White" 
                                   FontSize="50" 
                                   FontWeight="Black" 
                                   Margin="0,10,0,0" 
                                   TextWrapping="Wrap"
                                   LineHeight="46" 
                                   LineStackingStrategy="BlockLineHeight"/>

                        <StackPanel Orientation="Horizontal" Margin="0,15,0,0">
                            <Ellipse Width="26" 
                                     Height="26" 
                                     Fill="{StaticResource AccentGradient}" 
                                     Margin="0,0,10,0"/>
                            <TextBlock x:Name="AlbumAuthor" Text="Исполнитель" 
                                       Foreground="White" 
                                       FontWeight="SemiBold" 
                                       FontSize="15" 
                                       VerticalAlignment="Center"/>
                            <TextBlock Text=" • 12 треков, 45 мин." 
                                       Foreground="{StaticResource SecondaryText}" 
                                       FontSize="15" 
                                       Margin="6,0,0,0" 
                                       VerticalAlignment="Center"/>
                        </StackPanel>
                    </StackPanel>
                </Grid>
                <StackPanel Orientation="Horizontal" Margin="10,0,0,30">
                    <Button Style="{StaticResource PlayBtnStyle}" 
                            Width="55" 
                            Height="55">
                        <TextBlock Text="&#xE768;" 
                                   FontFamily="Segoe MDL2 Assets" 
                                   FontSize="26"
                                   Margin="4,0,0,0"
                                   VerticalAlignment="Center" 
                                   HorizontalAlignment="Center"/>
                    </Button>
                    <CheckBox Style="{StaticResource HeartToggleStyle}" 
                              Width="28" 
                              Height="28" 
                              Margin="25,0,0,0" 
                              VerticalAlignment="Center"/>
                </StackPanel>

                <TextBlock Text="Треки альбома" Foreground="White" FontSize="20" FontWeight="Bold" Margin="0,20,0,10"/>

                <ContentControl Style="{StaticResource TrackListContainer}">
                    <ItemsControl x:Name="TracksList">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Style="{StaticResource TrackRowStyle}" 
                                        Click="TrackRow_Click" 
                                        DataContext="{Binding}"
                                        Tag="{Binding}"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ContentControl>

                <Border Height="80"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
