<Page x:Class="MusicAppFront.Views.Pages.FavoritesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MusicAppFront.Views.Pages"
      mc:Ignorable="d" 
      Title="FavoritesPage">

    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Hidden" 
                      HorizontalScrollBarVisibility="Disabled">
            <StackPanel Margin="40,20">
                <Grid Margin="0,0,0,30">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Width="150" 
                            Height="150" 
                            CornerRadius="20" 
                            Background="{StaticResource AccentGradient}">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="40" 
                                              Color="#7C4DFF" 
                                              Opacity="0.3" 
                                              ShadowDepth="0"/>
                        </Border.Effect>
                        <TextBlock Text="&#xE006;" 
                                   FontFamily="Segoe MDL2 Assets" 
                                   Foreground="White" 
                                   FontSize="60" 
                                   HorizontalAlignment="Center" 
                                   VerticalAlignment="Center"/>
                    </Border>

                    <StackPanel Grid.Column="1" 
                                VerticalAlignment="Bottom" 
                                Margin="35,0,0,10">
                        <TextBlock Text="ПЛЕЙЛИСТ" 
                                   Foreground="White" 
                                   FontSize="13" 
                                   FontWeight="Bold" 
                                   Opacity="0.5"/>
                        <TextBlock Text="Любимые треки" 
                                   Foreground="White" 
                                   FontSize="56" 
                                   FontWeight="Black" 
                                   Margin="0,5,0,0"/>

                        <StackPanel Orientation="Horizontal" 
                                    Margin="0,15,0,0">
                            <Ellipse Width="24" 
                                     Height="24" 
                                     Fill="#7C3AED"
                                     Margin="0,0,10,0"/>
                            <TextBlock Text="MAX_User" 
                                       Foreground="White" 
                                       FontWeight="SemiBold" 
                                       FontSize="14" 
                                       VerticalAlignment="Center"/>
                            <TextBlock Text=" • 128 треков, 6 ч. 45 мин." 
                                       Foreground="{StaticResource SecondaryText}" 
                                       FontSize="14" 
                                       Margin="5,0,0,0" 
                                       VerticalAlignment="Center"/>
                        </StackPanel>
                    </StackPanel>
                </Grid>

                <StackPanel Orientation="Horizontal" 
                            Margin="0,0,0,30">
                    <Button Style="{StaticResource PlayBtnStyle}" 
                            Width="55" 
                            Height="55">
                        <TextBlock Text="&#xE768;" 
                                   FontFamily="Segoe MDL2 Assets" 
                                   FontSize="26" 
                                   Margin="4,0,0,0" 
                                   VerticalAlignment="Center" 
                                   HorizontalAlignment="Center"/>
                    </Button>
                </StackPanel>

                <ContentControl Style="{StaticResource TrackListContainer}">
                    <ItemsControl x:Name="TracksList" ItemsSource="{Binding FavoriteTracks}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Style="{StaticResource TrackRowStyle}" 
                                    Command="{Binding DataContext.PlayTrackCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                    CommandParameter="{Binding}"
                                        Click="TrackRow_Click"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ContentControl>
                <Border Height="80"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
